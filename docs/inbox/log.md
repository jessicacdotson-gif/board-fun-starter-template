# Inbox — append-only capture

Newest entries at the bottom. Date every entry. Raw is fine — this is
capture, not prose. Consolidation passes move insights to `wiki/`,
`spec/`, or `decisions/` and mark the source entries `[consolidated]`.

---

## 2026-07-12 (example entry — replace with your own)

- Template initialized. Run `/setup` to configure for a new game.
- Example of what belongs here: "playtest note — resting a hand on a
  piece registers as a separate finger contact near the same spot;
  piece input probably needs a freshness window over finger input.
  Filed as open question in spec/requirements.md."
